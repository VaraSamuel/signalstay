from typing import List, Dict, Any


def search_similar_review_snippets(property_id: str, topic: str, top_k: int = 3) -> List[Dict[str, Any]]:
    """
    Placeholder for FAISS or embeddings-based retrieval.
    For now, returns an empty list so the rest of the app works.
    """
    return []